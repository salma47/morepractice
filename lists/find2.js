var animals = require("./animals.js");

// fill in the function findPrimate so that the console log on line 11 
// prints the animal whose species is "Primate"

const findPrimate = animal = {

};

const primate = animals.find(findPrimate);

console.log(primate);

// DO NOT DELETE
module.exports = primate;